{
  "name": "serverless-demo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "packageManager": "yarn@3.2.0",
  "scripts": {
    "start:frontend": "yarn workspace @serverless-demo/frontend start",
    "start:api": "yarn workspace @serverless-demo/dev-server start",
    "build": "yarn workspaces foreach run build",
    "test": "yarn workspaces foreach run test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
